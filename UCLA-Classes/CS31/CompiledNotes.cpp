��⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⡇ 
⢀⢀⢀⢀⢀⠘⣾⢸⢸⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣧ 
⢀⢀⢀⢀⢀⢀⠈⣹⣾⣿⣿⣿⣿⣿⣿⣿⣿⣿🍆⣿⣿⣿⣿⣇ 
⢀⢀⢀⢀⢀⢀⢀⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⡆ 
⢀⢀⢀⢀⢀⢀⣸⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⠿ 
⢀⢀⢀⢀⢀⢠⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣧ 
⢀⢀⢀⢀⢀⣸⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⡉⢓⣤⣀⡀ 
⢀⢀⢀⢀⢀⣿⢿⣿⣿⣿⣿⣿⠤⠤⠤⠽⡿⢿⣿⣿⣿⣿⣿⣿⣷⣶⣾⣿⣿⣷⣶⣶⣤⡄ 
⢀⢀⢀⢀⢀⢀⣼⣿⣿⣿⣿⣿⢒⣒⣂⣀⣉⣦⡀⠈⠉⠉⠙⠛⠛⠛⠋⠉⠉⠉⠉⠁⢸⠁ 
⢀⢀⢀⢀⢀⢀⡿⢿⣿⣿⣿⣿⣿⣿⣿⣿⣿⣿⡿⠟⡉⠉⠙⠛⠛⠛⠛⠛⠓⠒⠒⠒⠊ 
⢀⢀⢀⢀⢀⢀⠇⢀⠈⠉⠛⠛⠛⠛⠛⠛⠉⠁⢀⠠⡇ 
don't fucking Johnny Test me or you’ll have a bad time


Basics

Control Flow

Functions

Parameters
==================================================================================================================================================
								Strings
==================================================================================================================================================

-----------------------------
Reading Strings
-----------------------------

 If our program says

        cout << "How many place settings would you like to buy? ";
        int numberOfSettings;
        cin >> numberOfSettings;

        cout << "In which style? ";
        string style;
        getline(cin, style);

we end up with the empty string as the value of style. This is because the expression  cin >> numberOfSettings  consumes the 1 and 2 characters, but not the newline we typed right after the 2. The expression  getline(cin, style)  picks up right where the previous input operation left off, so it reads all (zero) characters up to that newline, and that newline itself, and stores the zero-character sequence in style. To throw away any input after the 12, we should have said

        cout << "How many place settings would you like to buy? ";
        int numberOfSettings;
        cin >> numberOfSettings;
        cin.ignore(10000, '\n');       // this is new

        cout << "In which style? ";
        string style;
        getline(cin, style);

The expression  cin.ignore(10000, '\n')  causes characters up to and including the next newline character to be consumed and discarded. Technically, it will consume and discard either all characters up to the next newline or 10000 characters, whichever comes first. We pick a huge number so that the latter situation will never occur. Notice that we use single quotes, not double quotes, to denote the single newline character here. 


-----------------------------
Comparing Strings
-----------------------------
	If both strings are the same length and have the same characters at the same point, they're equal. It compares letters based on their encoded values
	Ghostwriter
	Ghoul
	Ghostwriter < Ghoul
	The lengths don't matter, it's the encoded values of the characters at each location in the string.
	Creates the same order that a dictionary would.
	
	Ghostwriter > Ghost
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
											C++ Strings
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
C++ Strings are a type of object defined in the language

	#include <string>
	using namespace std;

	string t = "Ghost"
	string s; //empty string

	for (int k = 0; k != t.size; k++) //possible to use .size
		cout << t[k] << endl;
		
	cout << t;
	getline(cin, s); //can use getline

	s = t; //sets s to Ghost
	s += "!!!"; // sets s to Ghost!!!

	if (t < s) //compare like this
		...
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
											C Strings
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

C Strings are just arrays of chars
used because not everything is written in c++

strlen(s) //returns length of s not including \0
strcpy(t,s) //copies the string s to t
strncpy(t,s,n) //copies the first n characters of s to t
strcat(t,s) //appends s to the end of t
strcmp(t,s) //compares t and s, returns 0 if they're equal, > 0 if t > s, and < 0 if t < s


	if (strlen(str) + 2 > max) <- inserting 1 extra character. also the end \0 which is why it's +2
	
	//don't use strlen in for loops, because it runs in n time, causing the loop to run in n^2 time. store it in a seperate variable and reference that instead.


	#include <cstring>
	char t[10] = { 'G', 'h', 'o', 's', 't'); //this works, but isn't commonly used
	char t[10] = "Ghost"; //this does the same thing as the line above it
	char s[100] = "";

		
					for (int k = 0; k != strlen(t); k++) //don't do this
						cout << t[k] << endl;
		
	for (int k = 0; t[k] != '\0'; k++) // all C strings have a \0 at the end of them
		cout << t[k] << endl;
		
	cout << t;
	cin.getline(s, 100); //use cin.getline and pass the array size

					s = t; // gives you an error: can't assign arrays.
	strcpy(s, t); //strcpy(destination, source) - now s is Ghost
				//strcpy(t, "abcdefghij");

	strcat(s, "!!!");	//now s is "Ghost!!!"
		
					if (t < s) //this will compile properly, but does not function in the desired way
	... strcmp(a, b) ...
	//		returns:
	//			negative if a comes before b
	//			0 if a equals b
	//			positive if a comes after b
	
	//Is a equal to b?
		/*WRONG:*/ if (strcmp(a, b)) //this yields the opposite result?
		
		/*RIGHT:*/ if (strcmp(a, b) == 0)
	
	//Does a come before b?
		/*WRONG:*/ if (a < b) // will compile for C strings, but it doesn't do what you want
		
		/*RIGHT:*/ if (strcmp(a, b) < 0)
	
	
	//Calling functions that use C strings
		void f(const char cs[])
		{
			...
		}

		int main()
		{
			string s = "Hello";
			f(s); //won't compile, because this is a C++ string not a c string
			f(s.c.str()); //works ok
			
			chat t[10] = "Ghost";
			s = t; //s is now Ghost
			s = "Wow"
			t = s; //won't compile
			t = s.c.str(); //also won't compile
			strcpy(t, s.c.str()); //t is now Wow
		}
	
	//C Strings vs C++ Strings
	
		//C STRING VERSION
		void makeUpperCase(char s[])
		{
			for (int k = 0; s[k] != '\0'; k++)
				s[k] = toupper(s[k]);
		}
		//C++ STRING VERSION
		void makeUpperCase(string& s)
		{
			for (int k = 0; k != s.size(); k++)
				s[k] = toupper(s[k]);
		}

		// C STRING VERSION
		int countLength(const char a[][MAX_WORD_LENGTH + 1], int n, int targetLength)
		{
			int total = 0;
			for (int k = 0; k < n; k++)
			{
				if (strlen(a[k]) == targetLength)
					total++;
			}
			return total;
		}
		// C++ STRING VERSION
		int countLength(const string a[], int n, int targetLength)
		{
			int total = 0;
			for (int k = 0; k < n; k++)
			{
				if (a[k].size() == targetLength)
					total++;
			}
			return total;
		}
		
	
	
	
	
	
	
	
	
	
	
			
Arrays

Pointers

Null Pointer value: nullptr


==================================================================================================================================================
														Classes / Structs
==================================================================================================================================================

Classes: Used to introduce new data types and functions to c++
Struct: functionally the same as a class, the only difference is 

struct Employee
{
	string name;
	double salary;
	int age;
}; //semicolon at the end!

void printPaycheck(const Employee& e);
void celebrateBirthday(Employee* ep)

int main()
{
	Employee company[100];
	int nEmployees = 0;
	//read info to fill up the array, set nEmployees
	
	printPaycheck(company[0]);
	celebrateBirthday(&company[2]);
	cout << totalPayroll(company, nEmployees);
	
	for (Employee* ep = company.
		cout << ep->name << endl;
}

void printPaycheck(const Employee& e)
{
	cout << "Pay to the order of " << e.name << " the amount of $" << e.salary/12 << endl;
}

void celebrateBirthday(Employee* ep)
{
	ep->age++;
}


//The Caller's object shouldn't change:\
	Pass by value => best if objs are cheap to copy \
	Pass by const reference => best if they're not cheap
	
//The Caller's object should change:\
	Pass by nonconstant reference
	
//An object of some struct type followed by a . then the name of a member of that type
// object.member

//a pointer to an object of some struct type followed by -> then the name of a member of that type. ====> p->m == (*p).m

//The name of the struct type followed by :: followed by the name of a member of that type. =====>    target::move()


//in C++, there is no difference between a struct and a class. They're two different words for functionally the same thing. The only difference is one that 


// If you declare no constructor at all, the compiler writes a zero-argument constructor ("a default constructor") for you that leaves members of built-in types uninitialized and default-donstructs members of class types. "default constructor" == "zero-argument constructor"



class Toy
{
	...
};

class Pet
{
	public:
		Pet(string nm, int initialHealth); //constructor. called automatically when an obj is initialized.
		~Pet(); //the DESTRUCTOR. automatically called on an obj when that obj is about to go away.
		void addToy();
	private:
		string m_name;
		int m_health;
		Toy* m_favoriteToy;
};

Pet::Pet(string nm, int initialHealth)
{
	m_name = nm;
	m_health = initialHealth;
	//want to initialize the ptr to nullptr, otherwise it will be unassigned but not nullptr. This is so a toy is optional input
	m_favoriteToy = nullptr;
}

Pet::~Pet()
{
	//delete will never do anything bad if it gets passed a nullptr. It can handle it it's a big boy
	delete m_favoriteToy;
}

void Pet::addToy()
{
	delete m_favoriteToy
	m_favoriteToy - new Toy;
}

void f()
{
	Pet p("Frisky", 20);
	p.addToy();
		...
	...
	p.addToy();
	//can't delete or manipulate private variables in a nonmember function, so we need to call a member function that does it
	//way to automatically do this It has a special name and is automatically called.
	//p.cleanup();
}

//any data member of a class type gets its zero argument constructor called. ex string is a member, default constructor is called to initialize it. The compiler writes the constructor for us.



//Named local variables ("automatic variables") live on "the stack" in memory
//Variables declared outside of any function live in the "global storage area" ("The static storage area")
//dynamically allocated objects live on "the heap"

//This is an ALLEGORY for how the zombies in project 7 work. it's literally like the same code with different names

class Employee
{
	public:
		Employee(string nm, double sal);
		void receiveBonus(double rate) const;
	private:
		string m_name;
		double m_salary;
};

class Company
{
	public:
		Company();
		~Company();
		void hire(string nm, double sal);
		void setBonusRate(double rate);
		void giveBonuses() const;
	private:
		Employee* m_employees[100];
		int m_nEmployees;
		double m_bonusRate;
};

Employee::Employee(string nm, double sal)
{
	m_name = nm;
	m_salary = sal;
}

void Employee::receiveBonus(double rate)
{
	cout << "Pay to " << m_name << " $" << rate * m_salary << endl;
}

Company::Company()
{
	m_nEmployees = 0;
	m_bonusRate = 0;
}

Company::~Company()
{
	for (int k = 0; k < m_nEmployees; k++)
		delete m_employees[k];
}

void Company::hire(string nm, double sal)
{
	if (m_nEmployees == 100)
		...error
	m_employees[m_nEmployees] = new Employee(nm, sal);
	m_nEmployees++;
}

void Company::setBonusRate(double rate)
{
	m_bonusRate = rate;
}

void Company::giveBonuses() const;
{
	for (int k = 0; k < m_nEmployees; k++)
		m_employees[k]->receiveBonus(m_bonusRate);
}



class Complex
{
	public:
		Complex(double re, double im);
		Complex();	//can have more than one fcn with the same name as long as they are different somehow in their arguments/way to call them: overloading the function names. Can overload a fcn name 
		double real() const;
		double imag() const;
	private:
		double m_rho;
		double m_theta;
};

Complex::Complex(double re, double im)
{
	m_rho = sqrt(re*re + im*im);
	m_theta = atan(im, re);
}

Complex::Complex()
{
	m_rho = 0;
	m)theta = 0;
}

double Complex::real() const
{
	return m_rho * cos(m_theta);
}

double Complex::imag() const
{
	return m_rho * sin(m_theta);
}

int main()
{
	Complex c1(4, -3) // 4 - 3i
	cout << ci.real() << " then " << ci.imag(); // writes 4 then 
	Complex ca[100]; //all initialized to 0+0i
	
}

